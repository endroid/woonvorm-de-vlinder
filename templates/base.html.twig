<!doctype html>
<html lang="nl">
    <head>
        <meta charset="utf-8">
        <title>{{ currentRoute.title }} - Woonvorm De Vlinder Prinsenbeek</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <meta name="view-transition" content="same-origin">
        <meta http-equiv="Content-Security-Policy" content="script-src 'self' 'nonce-c4019b07fafe';">
        <script type="module" nonce="c4019b07fafe" src="/scripts/app.js"></script>
        <link rel="stylesheet" href="/styles/app.css">
    </head>
    <body id="{{ currentRoute.template }}">
        <div id="navigation">
            <ul>
                {% for path, route in routes %}
                    <li><a href="{{ path }}"{% if route.title == currentRoute.title %} class="active"{% endif %}>{{ route.title }}</a></li>
                {% endfor %}
            </ul>
            <div id="date" data-controller="date"></div>
        </div>
        <div id="content">
            {% macro title(title) %}<div id="title">{{ title }}</div><br />{% endmacro %}
            {% block content %}{% endblock %}
        </div>
    </body>
</html>
